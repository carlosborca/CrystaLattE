name: test
channels: 
  - conda-forge
  - conda-forge/label/libint_dev
dependencies:
    # Base depends
  - python
  - pip
  - psi4
  - pycifrw
  - pydantic < 2.0

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  #- pip:
  #  - codecov
