name: test
channels: 
  - psi4/label/dev
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - psi4
    #  - psi4-rt
  - pycifrw

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
