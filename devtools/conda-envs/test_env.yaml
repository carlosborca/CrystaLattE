name: test
channels: 
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - psi4 >= 1.9.1
  - pycifrw

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  #- pip:
  #  - codecov
